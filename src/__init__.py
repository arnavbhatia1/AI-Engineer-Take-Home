"""AI-powered TTB alcohol label verification — core logic package.

This package is deliberately UI-agnostic (no Streamlit imports) so the
verification engine and providers can be unit-tested and reused headless.
"""
